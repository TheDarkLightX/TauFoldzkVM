fa(a, b, cin, s, cout) := ex s1, c1, c2. (
    ha(a, b, s1, c1) &&
    ha(s1, cin, s, c2) &&
    cout = (c1 | c2)
).fa(a, b, cin, s, cout) := ex s1, c1, c2. (
    ha(a, b, s1, c1) &&
    ha(s1, cin, s, c2) &&
    cout = (c1 | c2)
).