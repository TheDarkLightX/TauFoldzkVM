# 16-bit AND decomposition (simplified)
# Test just the decomposition concept with fewer variables

# 4-bit AND using 2-bit chunks: 0xF & 0x0 = 0x0
solve a3=1 && a2=1 && a1=1 && a0=1 && b3=0 && b2=0 && b1=0 && b0=0 && rh=(a3&b3)|(a2&b2) && rl=(a1&b1)|(a0&b0) && result=((rh+1)&(rl+1))

quit